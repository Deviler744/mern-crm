services:
  - type: web
    name: mern-crm
    env: docker
    branch: main
    autoDeploy: true
    plan: starter
    dockerfilePath: Dockerfile
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
